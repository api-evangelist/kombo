{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "#/components/schemas/PostIntegrationsIntegrationIdSetupLinkPositiveResponse",
  "title": "PostIntegrationsIntegrationIdSetupLinkPositiveResponse",
  "type": "object",
  "properties": {
    "status": {
      "type": "string",
      "const": "success"
    },
    "data": {
      "type": "object",
      "properties": {
        "link": {
          "type": "string",
          "format": "uri",
          "description": "The setup link URL to pass to the Kombo Connect SDK."
        }
      },
      "required": [
        "link"
      ]
    }
  },
  "required": [
    "status",
    "data"
  ]
}